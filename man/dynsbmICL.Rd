% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper.R
\name{dynsbmICL}
\alias{dynsbmICL}
\title{Compute ICL for Models Returned by \code{select.dynsbm}}
\usage{
dynsbmICL(x)
}
\arguments{
\item{x}{A list returned by the legacy \code{dynsbm} package's
\code{select.dynsbm()} helper.}
}
\value{
A \code{data.frame} with one row per fitted model and the columns:
\describe{
\item{Q}{Number of groups.}
\item{logLik}{Completed-data log-likelihood stored in the fitted model.}
\item{ICL}{Recomputed ICL value.}
}
}
\description{
Recomputes the Integrated Classification Likelihood (ICL) values for the
models returned by the legacy \code{dynsbm} package's \code{select.dynsbm()} helper.
The implementation follows that package's internal \code{compute.icl()} function
used for plotting the ICL curve.
}
\details{
The ICL is computed using the formula implemented in the \code{dynsbm} package:

\deqn{
\mathrm{ICL} = \mathrm{loglikelihood}
- \mathbf{1}_{T > 1}\frac{1}{2}Q(Q - 1)N(T - 1)
- \mathrm{pen},
}

where, in the binary case,

\deqn{
\mathrm{pen} =
\frac{1}{2}Q\frac{N(N - 1)T}{2} +
\frac{1}{4}Q(Q - 1)T\frac{N(N - 1)}{2}.
}

For continuous models, the penalty is doubled, matching the package source.
If a model contains a \code{gamma} component (discrete edge type), the original
package does not provide ICL values, so \code{NA} is returned.
}
\examples{
\dontrun{
library(dynsbm)
data(simdataT5Q4N40binary)
sel <- select.dynsbm(simdataT5Q4N40binary, Qmin = 1, Qmax = 6, nstart = 1)
dynsbmICL(sel)
}

}
\references{
dynsbm source code for \code{select.dynsbm()}, \code{draw.icl()} and \code{compute.icl()}:
\href{https://rdrr.io/cran/dynsbm/src/R/select.dynsbm.R}{rdrr source}
}
