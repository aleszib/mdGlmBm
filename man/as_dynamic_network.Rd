% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamic_network.R
\name{as_dynamic_network}
\alias{as_dynamic_network}
\title{Convert time-indexed matrices to a dynamic network object}
\usage{
as_dynamic_network(Y, directed = TRUE, self_ties = FALSE, times = NULL)
}
\arguments{
\item{Y}{A non-empty named or unnamed list of square matrix-like objects.}

\item{directed}{Logical scalar. If \code{TRUE} (default), dyads are treated as
ordered pairs. If \code{FALSE}, dyads are stored once per unordered pair using
the upper triangle of each matrix.}

\item{self_ties}{Logical scalar. If \code{FALSE} (default), diagonal entries are
retained in the dyad table as unobserved rows with \code{value = NA} so diagonal
handling is explicit. If \code{TRUE}, diagonal ties remain observed when present.}

\item{times}{Optional time labels. If \code{NULL}, names from \code{Y} are used when
available; otherwise sequential labels \code{t1}, \code{t2}, ... are generated.}
}
\value{
An object of class \code{dynamic_network} with at least:
\describe{
\item{times}{Time labels.}
\item{actor_time}{Actor-time table.}
\item{dyads}{Dyad table.}
\item{lineage}{Identity lineage links between adjacent time points.}
\item{directed}{Logical directedness flag.}
\item{self_ties}{Logical self-tie handling flag.}
}
}
\description{
Build the package's internal dynamic data representation from a list of
square matrices with actor IDs in row and column names. Actors may enter or
leave across time points; only adjacent same-ID observations create lineage
links.
}
\examples{
Y <- list(
  t1 = matrix(c(0, 1, 0, 0), nrow = 2, byrow = TRUE),
  t2 = matrix(c(0, 1, 1, 0, 0, 1, 1, 0, 0), nrow = 3, byrow = TRUE)
)
dimnames(Y$t1) <- list(c("A", "B"), c("A", "B"))
dimnames(Y$t2) <- list(c("A", "B", "C"), c("A", "B", "C"))

dn <- as_dynamic_network(Y)
dn$actor_time
dn$lineage

}
