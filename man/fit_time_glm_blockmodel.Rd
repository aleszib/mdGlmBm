% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time_glm.R
\name{fit_time_glm_blockmodel}
\alias{fit_time_glm_blockmodel}
\title{Fit one time-specific GLM blockmodel}
\usage{
fit_time_glm_blockmodel(
  x,
  membership,
  time = NULL,
  family = c("binomial", "ppml"),
  formula = value ~ block
)
}
\arguments{
\item{x}{A \code{dynamic_network} object from \code{\link[=as_dynamic_network]{as_dynamic_network()}}.}

\item{membership}{Membership labels for actor-time units. Supported inputs
are a full actor-time vector, a matching data frame, or a list of
per-time memberships.}

\item{time}{The time point to fit. For multi-time networks this may be a
time label or one-based index. If omitted, \code{x} must contain exactly one
time point.}

\item{family}{GLM family specification. Supported values are \code{"binomial"}
and \code{"ppml"}.}

\item{formula}{Optional model formula. The default is \code{value ~ block}.}
}
\value{
A \code{time_glm_blockmodel} object with the fitted \code{glm} model, common
criterion fields, and normalized membership data for the selected time.
}
\description{
Fit the observation model for a single time point of a \code{dynamic_network}
object conditional on supplied actor-time memberships.
}
\examples{
Y <- list(
  t1 = matrix(c(0, 1, 1, 0), nrow = 2, byrow = TRUE)
)
dimnames(Y$t1) <- list(c("A", "B"), c("A", "B"))
dn <- as_dynamic_network(Y)
fit_time_glm_blockmodel(dn, membership = c(1L, 1L), family = "binomial")

}
