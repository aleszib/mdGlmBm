% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper.R
\name{balassaNorm}
\alias{balassaNorm}
\title{Balassa Normalization for a One-Mode Network}
\usage{
balassaNorm(x)
}
\arguments{
\item{x}{A numeric object coercible to a matrix.}
}
\value{
A numeric matrix of the same dimensions as \code{x}, containing Balassa
normalized values.
}
\description{
Applies Balassa normalization to a matrix representing a one-mode network.
The input is first coerced to a matrix using \code{\link[=as.matrix]{as.matrix()}}. The normalization
divides each observed entry by its expected value under independence of row
and column totals.
}
\details{
For an input matrix \code{x}, the expected value for cell \verb{(i, j)} is computed as
\code{rowSums(x)[i] * colSums(x)[j] / sum(x)}. The returned matrix is \code{x / expected}.
}
\examples{
m <- matrix(c(10, 5, 4, 8), nrow = 2, byrow = TRUE)
balassaNorm(m)

}
