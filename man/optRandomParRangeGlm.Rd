% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GLMblockmodelingFunctions.R
\name{optRandomParRangeGlm}
\alias{optRandomParRangeGlm}
\title{Optimize Random GLM Partitions Over a Range of Block Counts}
\usage{
optRandomParRangeGlm(M, maxK, rep, minK = 1, ...)
}
\arguments{
\item{M}{A matrix or network object passed to \code{optParGlm()} and
\code{optRandomParGlm()}.}

\item{maxK}{Integer scalar. Maximum number of clusters to evaluate.}

\item{rep}{Integer scalar. Number of random repetitions passed to
\code{optRandomParGlm()} for each value of \code{k}.}

\item{minK}{Integer scalar. Minimum number of clusters to evaluate. Defaults
to \code{1}. If \code{minK = 1}, the one-cluster solution is fitted using a constant
partition and the random search starts at \code{k = 2}.}

\item{...}{Additional arguments passed to \code{optParGlm()} and
\code{optRandomParGlm()}.}
}
\value{
A list with elements:
\describe{
\item{bestPart}{The partition of the best-fitting solution.}
\item{bestFit}{The highest ICL value found.}
\item{ICL}{A named vector of ICL values for each evaluated number of clusters.}
\item{bestRes}{The full result object corresponding to the best solution.}
\item{searchHistory}{A list of result objects for all evaluated values of \code{k}.}
}
The elapsed computation time is stored as the \code{"time"} attribute of the
returned object.
}
\description{
Fits GLM blockmodel partitions for a range of numbers of clusters and selects
the solution with the highest ICL value. For each \code{k} from \code{minK} to \code{maxK},
the function runs \code{optRandomParGlm()}, except when \code{minK = 1}, where the
one-cluster solution is fitted directly with \code{optParGlm()}.
}
\seealso{
\code{optParGlm()}, \code{optRandomParGlm()}
}
