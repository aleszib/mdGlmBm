% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transition_scoring.R
\name{score_actor_time_candidates}
\alias{score_actor_time_candidates}
\title{Score candidate memberships for one actor-time unit}
\usage{
score_actor_time_candidates(
  x,
  fit,
  membership = NULL,
  unit_id = NULL,
  row_index = NULL,
  candidate_clusters = NULL,
  transition = NULL,
  prior = c("none", "uniform", "empirical"),
  prior_smoothing = 0.5,
  smoothing = 0.5,
  return_components = TRUE
)
}
\arguments{
\item{x}{A \code{dynamic_network} object from \code{\link[=as_dynamic_network]{as_dynamic_network()}}.}

\item{fit}{A fitted time-GLM object from \code{\link[=fit_time_glm_blockmodel]{fit_time_glm_blockmodel()}} or
\code{\link[=fit_time_glm_blockmodels]{fit_time_glm_blockmodels()}}.}

\item{membership}{Current actor-time memberships. When omitted, the
memberships stored in \code{fit} are used if they align with \code{x$actor_time}.}

\item{unit_id}{Optional actor-time \code{unit_id} identifying the target row.}

\item{row_index}{Optional one-based row index into \code{x$actor_time}.}

\item{candidate_clusters}{Candidate cluster labels. Defaults to all labels
implied by the transition or prior object, or by the current memberships.}

\item{transition}{Optional pre-estimated Markov transition object. If not
supplied, \code{estimate_markov_transitions()} is called.}

\item{prior}{Optional membership prior specification or
\code{membership_prior} object.}

\item{prior_smoothing}{Additive smoothing used when estimating an empirical
membership prior.}

\item{smoothing}{Additive smoothing used when estimating transitions if no
transition object is supplied.}

\item{return_components}{Logical; kept for future compatibility. The
reference implementation always returns the component columns.}
}
\value{
A data frame with one row per candidate cluster and at least:
\code{unit_id}, \code{actor_id}, \code{time}, \code{time_index}, \code{candidate_cluster},
\code{glm_deviance}, \code{previous_transition_penalty}, \code{next_transition_penalty},
\code{prior_penalty}, and \code{total_score}.
}
\description{
Compute deviance-scale local scores for assigning a single actor-time unit
to candidate clusters. The score combines the time-specific GLM deviance
contribution with Markov transition penalties and an optional membership
prior penalty.
}
\examples{
Y <- list(
  t1 = matrix(c(0, 1,
                1, 0), nrow = 2, byrow = TRUE),
  t2 = matrix(c(0, 1,
                1, 0), nrow = 2, byrow = TRUE)
)
dimnames(Y$t1) <- list(c("A", "B"), c("A", "B"))
dimnames(Y$t2) <- list(c("A", "B"), c("A", "B"))
dn <- as_dynamic_network(Y)
mem <- c(1L, 2L, 2L, 1L)
fit <- fit_time_glm_blockmodels(dn, membership = mem, family = "binomial")
score_actor_time_candidates(dn, fit, membership = mem, row_index = 1L)

}
