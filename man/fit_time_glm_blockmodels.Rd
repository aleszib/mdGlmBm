% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time_glm.R
\name{fit_time_glm_blockmodels}
\alias{fit_time_glm_blockmodels}
\title{Fit time-specific GLM blockmodels across all time points}
\usage{
fit_time_glm_blockmodels(
  x,
  membership,
  family = c("binomial", "ppml"),
  formula = value ~ block
)
}
\arguments{
\item{x}{A \code{dynamic_network} object from \code{\link[=as_dynamic_network]{as_dynamic_network()}}.}

\item{membership}{Membership labels for actor-time units. See
\code{fit_time_glm_blockmodel()} for accepted formats.}

\item{family}{GLM family specification. Supported values are \code{"binomial"}
and \code{"ppml"}.}

\item{formula}{Optional model formula. The default is \code{value ~ block}.}
}
\value{
A \code{time_glm_blockmodels} object with per-time fits and aggregated
criterion summaries.
}
\description{
Fit the observation model independently at each time point of a
\code{dynamic_network} object using supplied actor-time memberships.
}
\examples{
Y <- list(
  t1 = matrix(c(0, 1, 0, 0), nrow = 2, byrow = TRUE),
  t2 = matrix(c(0, 1, 1, 0, 0, 1, 1, 0, 0), nrow = 3, byrow = TRUE)
)
dimnames(Y$t1) <- list(c("A", "B"), c("A", "B"))
dimnames(Y$t2) <- list(c("A", "B", "C"), c("A", "B", "C"))
dn <- as_dynamic_network(Y)
mem <- c(1L, 1L, 1L, 1L, 2L)
fit_time_glm_blockmodels(dn, membership = mem, family = "binomial")

}
